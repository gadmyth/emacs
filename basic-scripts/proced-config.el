;;; package --- proced-config.el
;;; Commentary:
;;; Code:

(require 'proced)

(setq-default proced-auto-update-flag t)
(setq-default proced-auto-update-interval 3)
(setq-default proced-format 'verbose)


(provide 'proced-config)
;;; proced-config.el ends here
