;;; package --- python-config.el
;;; Commentary:
;;; Code:

(require 'python)
;; used for run-python command
(setq python-shell-interpreter "python3")

(provide 'python-config)
;;; python-config.el ends here