;;; package --- uniquify-config.el
;;; Commentary:
;;; Code:

(setq uniquify-buffer-name-style 'post-forward)
(setq uniquify-separator " :: ") 
(setq uniquify-after-kill-buffer-p t)

(provide 'uniquify-config)
;;; uniquify-config.el ends here
